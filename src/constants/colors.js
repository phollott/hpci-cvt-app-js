// colors
// /////////////////////////////////////////////////////////////////////////////

const blue = '#024b9e'; // saturation of darkColor
const darkColor = '#404F60'; // primary color (e.g. header, footer, icon...)
const darkHighlightColor = '#3a3a3a'; // (e.g. container dark background)
const green = '#52c518'; // (e.g. new badge)
const grey = '#848484'; // (e.g. drawer text)
const lightGreen = '#e5f2e5'; // (e.g. row highlight)
const lightGrey = '#f2f2f2'; // primary background color on screens and drawer
const orange = '#ff9f40'; // (e.g. updated badge)
const white50 = 'rgba(255, 255, 255, 0.5)'; // (e.g. inactive tab icons)

export default {
  black: '#000000',
  white: '#ffffff',
  blue,
  darkColor,
  darkHighlightColor,
  green,
  grey,
  lightGreen,
  lightGrey,
  orange,
  white50
};
